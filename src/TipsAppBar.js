import React from 'react';
import AppBar from 'material-ui/AppBar';
import './App.css'

const TipsAppBar = () => (
  <AppBar
    class='app-bar'
    title="Ledger Sheet Review"
    showMenuIconButton={false}
  />
);

export default TipsAppBar;